import React, { useState } from "react";
import ReactDOM from "react-dom";

function App() {
  console.log(1111);
    return (
      <div>hello wosad rld</div>
    );
}

window.addEventListener("load", () =>
  ReactDOM.render(<App />, document.getElementById("app"))
);